# Dependencies
node_modules/

# Test scratch profile
.test-tmp/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Build output — generated by tsdown via prepare (git install runs it)
lib/*
# The server half (lib/index.js) is a hand-maintained bundle without a source
# tree; prepare cannot rebuild it, so it must stay committed.
!lib/index.js
